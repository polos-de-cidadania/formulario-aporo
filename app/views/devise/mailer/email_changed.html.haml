%p Olá #{@resource.name}!
- if @resource.try(:unconfirmed_email?)
  %p Estamos entrando em contato para notificar que seu e-mail está sendo alterado para #{@resource.unconfirmed_email}.
- else
  %p Estamos entrando em contato para confirmar que você mudou seu e-mail para #{@resource.email}.
